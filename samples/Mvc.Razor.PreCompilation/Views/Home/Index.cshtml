Hello world, I was precompiled.

@{ 
    // Uncomment this line to see compilation errors at build time.
    // Without pre compilation they will only show up at run time.
    // int x = "Must be int";
}